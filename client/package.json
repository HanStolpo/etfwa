{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.js",
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "node-libs-browser": "^0.5.2",
    "purs-loader": "^0.4.0",
    "webpack": "^1.12.6",
    "css-loader": "^0.23.0",
    "html-webpack-plugin": "^1.6.2",
    "style-loader": "^0.13.0",
    "webpack-dev-server": "^1.12.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webpack-dev-server",
    "build": "./node_modules/.bin/webpack",
    "clean-all": "rm -rf build && rm -rf output",
    "clean-all": "rm -rf bower_components && rm -rf bundle.js && rm -rf node_modules && rm -rf output"
  },
  "author": "",
  "license": "ISC"
}
